services:
    nginx:
        container_name: fs-table
        image: fs-table-image
        build:
            context: .
            dockerfile: Dockerfile
        restart: unless-stopped
        networks:
            - feedback-system-network

networks:
    feedback-system-network:
        external: true
        name: feedback-system-network
